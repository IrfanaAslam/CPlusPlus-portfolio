# Library Management System

A console-based library catalog manager demonstrating classes, STL containers, and file I/O.

## Features
- Add books to the catalog
- Borrow / return books with availability tracking
- Export the catalog to CSV

## Build & Run
```bash
g++ main.cpp -o library_management
./library_management
```
